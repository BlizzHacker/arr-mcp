# Normalise to LF in the repository. Without this, a shell script authored on
# Windows lands in the Linux container with CRLF endings and fails at runtime
# with "bad interpreter: no such file or directory".
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.gz binary
*.db binary
